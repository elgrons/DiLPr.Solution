@{
  Layout = "_Layout";
}

@using DiLPr.Models;

  <h1>Checkout new pupprs based on your interest</h1>

  @if (@Model.Count == 0)
  {
    <h3>No tags have been added yet!</h3>
  } 

  @foreach (Tag tag in Model)
  {
    <li>@Html.ActionLink($"{tag.Name}", "Details", new { id = tag.TagId })</li>
  }

<br />
  @Html.ActionLink("Add new tag", "Create")